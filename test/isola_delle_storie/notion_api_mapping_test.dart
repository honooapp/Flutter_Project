// test/isola_delle_storie/notion_api_mapping_test.dart
import 'package:flutter_test/flutter_test.dart';

// Questo test resta in pausa finché non confermi come si istanzia/mappa davvero APIExercise.
// Per attivarlo: dimmi se esiste una factory (es. APIExercise.fromDb / fromMap / fromNotion)
// o qual è il costruttore con i named corretti, e lo aggiorno 1:1.

void main() {
  test('APIExercise mapping – in attesa della firma reale', () {
    expect(true, isTrue);
  }, skip: 'APIExercise non espone fromJson. Passami la firma/constructor reale per attivare il test.');
}
